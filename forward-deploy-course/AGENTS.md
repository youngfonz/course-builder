# AGENTS.md

This course can be run by any coding agent that reads this file (Codex, and
others), not only Claude Code. The instructor instructions live in `CLAUDE.md`.

**Read `CLAUDE.md` now and follow it exactly**, treating every reference to
"Claude Code" as a reference to you. Where it names the AskUserQuestion tool,
use your equivalent for multiple-choice questions, or ask in plain chat with
lettered options. Every other rule applies unchanged, especially the data rule,
the authority rule, and "never show code".
